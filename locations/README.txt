Here we can put code specific to one location / project.

Note that you shouldn't put large files in git -- e.g. the pile of netcdf forcing files, etc.

But it's a good place to put the configuration, etc. files, for future reference.

The `example` dir has, well, an example :-)

